<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.declarerequestpermissionslocation">
<!-- Here we have to declare the permissions needed for our app -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeclareRequestPermissionslocation">
        <activity
            android:name="com.example.declarerequestpermissionslocation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
<!--    this must be enable if there is no hardware in the device for location gps etc-->
<!--    <uses-feature -->
<!--        android:name="android.hardware.location"-->
<!--        android:required="false"/>-->

</manifest>